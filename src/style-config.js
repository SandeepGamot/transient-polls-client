export const styles = {
  w: {
    default: "w-11/12",
    md: "w-/12",
    lg: "w-9/12",
    max: "max-w-xl"
  }
};

export const w_all = `${styles.w.default} md:${styles.w.md} lg:${styles.w.lg} ${styles.w.max}`;
